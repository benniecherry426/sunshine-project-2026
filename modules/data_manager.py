import pandas as pd\n\nclass DataManager:\n    def __init__(self, filename):\n        self.filename = filename\n        self.data = None\n\n    def create_spreadsheet(self, data, sheet_name='Sheet1'):\n        '''Creates a spreadsheet with the given data.'''
        self.data = pd.DataFrame(data)\n        with pd.ExcelWriter(self.filename) as writer:\n            self.data.to_excel(writer, sheet_name=sheet_name, index=False)\n        print(f'Spreadsheet {self.filename} created with the data.')\n\n    def load_data(self, sheet_name='Sheet1'):\n        '''Loads data from the specified sheet of the spreadsheet.'''
        self.data = pd.read_excel(self.filename, sheet_name=sheet_name)\n        print(f'Data loaded from {self.filename} on sheet {sheet_name}.')\n\n    def get_data(self):\n        '''Returns the loaded data.'''
        return self.data\n\n    def save_data(self):\n        '''Saves the current data to the spreadsheet.'''
        if self.data is not None:\n            with pd.ExcelWriter(self.filename, mode='a', if_sheet_exists='replace') as writer:\n                self.data.to_excel(writer, sheet_name='Sheet1', index=False)\n            print(f'Data saved to {self.filename}.')\n        else:\n            print('No data to save.'),\n